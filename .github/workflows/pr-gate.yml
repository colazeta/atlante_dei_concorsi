name: PR gate

on:
  schedule:
    - cron: '*/30 * * * *'
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review, labeled, unlabeled]
  workflow_dispatch:
    inputs:
      pr_number:
        required: false
        type: string
      apply:
        required: false
        default: 'true'
        type: choice
        options: ['false','true']

permissions:
  contents: write
  pull-requests: write
  checks: read
  statuses: read

jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Resolve inputs
        id: r
        env:
          EVENT_NAME: ${{ github.event_name }}
          INPUT_PR: ${{ inputs.pr_number }}
          INPUT_APPLY: ${{ inputs.apply }}
          EVENT_PR: ${{ github.event.pull_request.number }}
        run: |
          if [ "$EVENT_NAME" = "schedule" ]; then
            echo "mode=all" >> "$GITHUB_OUTPUT"
            echo "pr=" >> "$GITHUB_OUTPUT"
            echo "apply=true" >> "$GITHUB_OUTPUT"
          elif [ "$EVENT_NAME" = "workflow_dispatch" ] && [ -z "$INPUT_PR" ]; then
            echo "mode=all" >> "$GITHUB_OUTPUT"
            echo "pr=" >> "$GITHUB_OUTPUT"
            echo "apply=${INPUT_APPLY:-true}" >> "$GITHUB_OUTPUT"
          elif [ "$EVENT_NAME" = "workflow_dispatch" ]; then
            echo "mode=one" >> "$GITHUB_OUTPUT"
            echo "pr=$INPUT_PR" >> "$GITHUB_OUTPUT"
            echo "apply=${INPUT_APPLY:-true}" >> "$GITHUB_OUTPUT"
          else
            echo "mode=one" >> "$GITHUB_OUTPUT"
            echo "pr=$EVENT_PR" >> "$GITHUB_OUTPUT"
            echo "apply=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Run gate
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ "${{ steps.r.outputs.mode }}" = "all" ]; then
            gh pr list --state open --json number --jq '.[].number' | while read PR; do
              [ -z "$PR" ] && continue
              ARGS=(--pr "$PR")
              if [ "${{ steps.r.outputs.apply }}" = "true" ]; then ARGS+=(--apply); fi
              python3 scripts/governed_auto_merge_gate.py "${ARGS[@]}"
            done
          else
            ARGS=(--pr "${{ steps.r.outputs.pr }}")
            if [ "${{ steps.r.outputs.apply }}" = "true" ]; then ARGS+=(--apply); fi
            python3 scripts/governed_auto_merge_gate.py "${ARGS[@]}"
          fi
      - name: Summary
        if: always()
        run: |
          if [ -f reports/auto-merge-gate/latest_state.json ]; then
            cat reports/auto-merge-gate/latest_state.json >> "$GITHUB_STEP_SUMMARY"
          fi
